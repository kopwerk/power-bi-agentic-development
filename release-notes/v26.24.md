# v26.24

- Power BI Desktop bridge integration in the `pbir-cli` skill: drive a running Desktop instance with `pbir desktop list/refresh/screenshot`, close the loop locally (mutate, refresh, screenshot, inspect) instead of publishing to a sandbox, auto-refresh on save via `PBIR_DESKTOP_AUTO_REFRESH`, absolute report paths, and live local-engine query routing for thick reports; full workflow and troubleshooting in the new `references/desktop-integration.md`
- `connect-pbid` learns the bridge: port-to-report correlation (`pbir desktop list` plus the msmdsrv process tree), bridge-first open-file discovery, and a closing-the-loop section (engine check via `pbir model -q`, visual check via `pbir desktop screenshot`). Reload semantics verified empirically: `pbir desktop refresh` hot-reloads the report definition, but theme files and TMDL need a close and reopen
- `connect-pbid` script fixes: `create-field-parameter.ps1` built a non-functional field parameter through two nonexistent TOM APIs while reporting success (now uses `GroupByColumns` + `JsonExtendedProperty`, fails fast, and cleans up); `debug-dax.ps1` polls for async DAXEvaluationLog delivery instead of missing events; `daxlib.sh` errors clearly when `jq` is missing and supports `--help`; the quickstart dedupes ports and skips thin-report engines; `load-tmdl.ps1` warns when `-AddMeasure` runs without `-Save`
- pbi-desktop hooks: `refresh-cache` and `check-ri` now read executed `.ps1` contents (they never fired for the recommended `-File` workflow); the validate hooks no longer block the plugin's own bundled scripts; hook README synced to `hooks.json`
- `pbir-format`: repaired the bundled K201 example (phantom `pages.json` entries, dangling theme reference, stale bookmark sections and queryRef) in both plugin copies; resolved the `midColor` token contradiction (use `neutral`); refreshed schema-version drift notes
- `tmdl-validate` binaries rebuilt at v0.2.0; clarified TMDL indentation spec vs PBIP convention in the tmdl docs
